/*
var Datastore = require('nedb');
var db = new Datastore({pr_db:'/db'});
db.loadDatabase(function(err){
    // Start issuing commands after callback...
});
*/

var Datastore = require("nedb");
const internal = require("stream");
var user_db = new Datastore("db/user.db");
user_db.loadDatabase();

/*var test = new Datastore({filename:"db/test", autoload : true});
var man2={
    name_man:"",
    twitter:""
};
test.insert(man2,function(err, doc){

})
*/


    

function IncertRowsUser() {
    user_db.loadDatabase();
    alert("c2");
    var Second_n = document.getElementById("tx1").value;
    var First_n = document.getElementById("tx2").value;
    var Father_n = document.getElementById("tx3").value;
    var mail = document.getElementById("tx4").value;
    var pas = document.getElementById("tx5").value;
    var tel = document.getElementById("tx6").value;
    var birth = document.getElementById("tx7").value;

    var user={
        Second_name: Second_n,
        First_name: First_n,
        Father_name: Father_n,
        email: mail,
        password: pas,
        telephone: tel,
        date_of_birth: birth
    };

    user_db.insert(user, function(err, doc){

    })
}

